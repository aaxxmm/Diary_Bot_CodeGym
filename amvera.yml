meta:
  environment: docker
  version: "1.0"

build:
  type: docker
  dockerfile: Dockerfile

run:
  command: python main.py
  persistence:
    - /app/data

environment:
  - name: PYTHONUNBUFFERED
    value: "1"